<!-- Content Heading -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else { res.redirect('/') } %> <%- messages() %> <% if
(errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %> <% if (error.msg
  != 'Invalid value') { %>

  <li><%= error.msg %></li>
  <% }}) %>
</ul>
<% } %>

<p>
  <a href="../../inv/add-classification" class="button">
    Add New Classification
  </a>
</p>
<p>
  <a href="../../inv/add-vehicle" class="button">
    Add New Vehicle
  </a>
</p>
<h2>Manage Inventory</h2>
<select name="classifications" id="classificationList">
  <option value="">Select a classification</option>
  <%- classificationOptions %>
</select>
<table id="inventoryDisplay"></table>
<noscript>
  JavaScript must be enabled to use this page.
</noscript>
<script src="../../js/inventory.js"></script>
